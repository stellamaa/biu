/** Landing desktop body — 12px at lg, +2 sizes from 3xl / 2K+ (text-base). */
export const landingDesktopBodyTextClass =
  'text-[12px] leading-tight 3xl:text-base 3xl:leading-tight'

/** Landing desktop labels — text-base at lg, +2 sizes from 3xl / 2K+ (text-xl). */
export const landingDesktopLabelTextClass =
  'text-base leading-none tracking-wide 3xl:text-xl'

/** Landing desktop logo bump from 3xl / 2K+ (+2 sizes from lg 25px). */
export const landingDesktopLogoTextClass = '3xl:text-4xl'

/** Landing language toggle track from 3xl / 2K+. */
export const landingDesktopToggleSizeClass =
  '3xl:h-[1.875rem] 3xl:w-[5.25rem]'
